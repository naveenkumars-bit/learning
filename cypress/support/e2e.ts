// Import custom commands for support
import './commands';

// Alternatively, you can add hooks that run before each test run
beforeEach(() => {
  // E.g., clear localStorage or session cookies if necessary
});
